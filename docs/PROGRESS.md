# 开发进度日志

> 每夜收口时追加一条：日期 / 阶段 / 完成 / 遗留 / 下一步。最新在最下。

| 日期 | 阶段 | 完成 | 遗留 / 下一步 |
|------|------|------|----------------|
| 2026-09-08 (通宵迭代收口) | **串标风险规则矩阵 15 条 + 真实资料试验闭环 + v0.21.0/v0.21.1/v0.22.0 三连发** | 与 Codex 监督线协作完成通宵迭代：①规则矩阵扩至 F-01~F-07/E-01~E-02/S-01~S-05/P-01~P-04 共 15 条（新增报价等差等比、控制价下浮率一致、清单构成、分块指纹、人员重合、亲属线索、保证金账户、企查查长表、F-07 接近带），全部人工复核线索；②真实资料只读试验（洺悦御府一标段 3 家 PDF 经济标 13~272MB + 二标段 2 家 .xls 清单）暴露并当天修复：说明行假报价、F-01 阈值漏报真实陪标对（→F-07）、PDF 直读与大文件预算；③回验原生 PDF 直读命中标定陪标对（好画↔泰宇 0.53%）；④三版本全经三平台 CI + 五件产物 Release（v0.22.0 Latest）；⑤协作机制：ZCode↔Codex 文件对接板 + 写入权协议，经历快照覆盖事故后转 commit 增量协作；测试 287→325+1 skipped | 中创投标总价表在 500 页提取上限外（如实显示未识别，未硬造）；GCFX（广联达加密格式）与 .doc 施组仍不支持；LAN 入口无鉴权；真实官网联调 P3R 仍待白天人工；建议权限人员准备匿名 A/B/C 样本作标准验收集；双平台包真机业务验收待用户 |
| 2026-09-07 (离线投标文件预警) | **投标审查器离线多投标文件预警第一版完成** | 新增 `app/offline_review.py`、`bqc review-bids` 与 Web `/review-bids`：支持 txt/md/csv/json/xlsx；报价接近/低离散/离群、清单同步、文本/结构/元数据相似、本地关联线索；输出带 SHA-256、定位、规则编号（F-01~F-04/E-01/S-01~S-03/P-01）的 JSON/Markdown，全部人工复核，不自动定性；产品显示名更新为“投标审查器”，旧 `bqc` 入口保留；F-05/F-06/S-04/P-02/P-03 已在后续收口接入，见最新记录 | **暂无已确认的“同一项目、多家单位投标”真实测试资料**；不读取/复制用户本机候选资料，不把演示/合成资料当作真实证据；后续如有权限人员提供脱敏资料再做离线回放 |
| 2026-09-04 (日间) | 初始化 | 仓库骨架：README/WORKPLAN/LICENSE/.gitignore/CI 占位/Issue 模板；任务书备份至 local/taskdoc/；已推 GitHub；执行模型定为闲时接力（至 9/20，每日 08:30 收口） | 首条闲时任务待当晚取号额度恢复（00:00 重置）后创建：P1 架构骨架 |
| 2026-09-04 (晚·主会话) | P2 完成 | 本地 Web UI 全链路：FastAPI + Jinja2 三页面（首页表单/结果页/重跑）；`bqc serve` 启动（127.0.0.1）；8 种 mock 场景（4 条款 FAIL/历史解除 WARNING/证照表面过期/查询失败 ERROR/无记录 NO_DATA）端到端跑通 runner→SQLite→结果页；测试 52 项全绿（含：ERROR/NO_DATA 绝不显示"正常"）。环境注意：editable 安装因网络失败，venv 直装 pyyaml+pytest 从仓库根跑 pytest 即可 | 下一轮 P3：全国数据源 adapter 骨架（creditchina/zxgk/mem/jzsc/pcczdc，mock 测试先行，真实 URL 复核留白天）。另：投标人 00:35 代建任务待新会话照桌面 ZCODE 备份重建，否则今晚闲时接力不会启动 |
| 2026-09-04 (晚·主会话二轮) | P3 完成 | 6 个全国源 adapter 骨架（gsxt/creditchina/zxgk/mem/jzsc/pcczdc）：SSRF 基座（含数字型/十六进制/IPv4-mapped 绕过形态拦截+DNS 复检）、传输层状态映射（TIMEOUT/ERROR/BLOCKED 永不 PASS，解析失败=ERROR）、query_url 未复核一律 MANUAL、gsxt 验证码源恒 MANUAL；抽取 Finding 与五规则联动验证；测试 52→99 全绿；CHANGELOG 0.3.0 | 各源 query_url 留空待白天人工复核（复核后才回填+修解析器）；runner 真实链路接入（nightly_mock_only 门控）留下一轮；**本轮 GitHub 不可达（Clash 核心未运行、直连 DNS 失败），push 待网络恢复补推**。闲时接力已挂 00:35 一次性定时（automation-f9f4358f），今晚自动取号建「投标人核查闲时续跑」。下一步：runner 接入真实链路完成 P3 收尾，或直接 P4 电建禁入 adapter（mock 先行） |
| 2026-09-04 (晚·主会话三轮) | P3 收尾完成 | runner 真实链路：run_check(real_sources=True) 逐源执行注册表 adapter（get 注入可测、nightly_mock_only 门控拒绝夜间真实查询）；数据源失败状态折算进总体结论（MANUAL/TIMEOUT 等绝不归约 PASS）；修复红线问题——mock query_error 场景总体结论原被吞成 NO_DATA，现正确=ERROR（source_queries 也记真实状态）；测试 99→104 全绿 | push 仍待网络（GitHub 不可达，c8bb1ce 与本轮提交待补推）；白天待办：各源 query_url 复核回填、zxgk/gsxt 验证码联调、Web 层真实模式入口。下一步：P4 电建禁入 adapter（mock 先行） |
| 2026-09-04 (晚·P0 修复轮) | **P0 仓库基线修复完成** | ①修安装：pyproject `packages={include=[...]}` 写法非法致 pip install 必失败（旧 PROGRESS"网络失败"系误诊，CI 靠 `\|\|` 兜底掩盖），改 packages.find 自动发现 app*；②config/ 移入 app/config 随包分发 + MANIFEST.in/package-data 保证模板与配置进 wheel/sdist（实测制品内 6 资源全在位）；③Web 数据库默认路径改当前工作目录（安装版不写 site-packages）；④版本统一 0.3.0 单点来源（pyproject/app.**version**/DB 种子/CLI）；⑤CI 重写：删解析期失败的 job 级 if 与安装 `\|\|` 兜底，test job（win/ubuntu/mac×py3.12）加 pip check+安装烟测+CLI smoke，新增 build job（python -m build→wheel/sdist 各装隔离 venv 脱离源码目录跑烟测）；⑥Windows 实跑暴露真产品缺陷两连：cp1252 控制台写中文崩溃 + 父进程按本地代码页解码中文 UTF-8 管道输出致读线程静默死亡（stdout=None，--version 纯 ASCII 能过即指纹）→ CLI 编码契约=管道强制 UTF-8/真控制台保本地代码页+errors=replace，测试显式 utf-8 解码；⑦测试 104→108 全绿（新增 test_packaging 3 项+legacy codepage 红测）；**CI run 33892860799 四 job（三平台 test+build）实跑全绿**；v0.3.0 已打 tag 发 Release（挂 wheel+sdist） | 遗留：pytest 直接从仓库根跑（不装包）不再受支持——P0 后安装是基线（test_packaging 会给出明确指引）；下一步：P4 电建禁入 adapter（mock 先行），任务书已注明 P0 后停止不自行进入 |
| 2026-09-05 (P0.5 修复轮) | **P0.5 可靠性闭环+真实样本回放完成** | §三 状态合并分层闭环（数据异常/人工要求不再被 WARNING 掩盖）；§四 httpx 异常映射+单源隔离（403/429/503→BLOCKED）；§五 run_id 批次隔离+0.3.x 幂等迁移；§六 主体一致性进真实链路（同名不同码不误并、缺码转人工、字段留痕）；§七 行业门控+NOT_APPLICABLE；§八 Project.terms 真正控制规则（scope=term/background 三层语义）；§九 SSRF 重定向逐跳校验（公网→内网全拦）；测试 108→194 全绿。真实样本回放：MobileDrive7 挂载（钥匙串凭据），16 家样本（招标台账/履约评价/资格审查 PDF 来源）端到端 16/16=MANUAL（96 源查询全 MANUAL，红线行为）；官方三站实测反自动化拦截（40001/加密体/521）与程序 MANUAL 预判一致；差异根因 D/F 类为主，新增 A/B/C 缺陷 0；真实数据只读零改动，样本文件全部留在 gitignored local/ 未提交 | 遗留：解析层真实数据验证需 P3R（query_url 人工复核回填+真机联调）；台账无 USCC 列（E 类靠 PDF 文字层提取，待官方复核）；下一步 P4 电建禁入 adapter（任务书令本轮后停止） |
| 2026-09-05 (P4 轮) | **P4 集团数据源完成** | app/sources/owners/powerchina.py：电建三级禁入名单 adapter（source_id=powerchina_ban）；registry automation_mode=manual_intake——内部名单公开门户不可自动核验，查询恒 MANUAL（模式注记优先于 URL 注记）；人工导入 parse 契约（subject_name/uscc+list_level 三级+scope+起止+document_name 证据溯源字段）经主体一致性关卡后产出 owner_ban Finding，条款4 离线评判（有效期内 FAIL/过期 WARNING/他集团 NO_DATA/缺码转人工全测）；engine 纵深加固：DIFFERENT_SUBJECT 在任何入口都不得形成证据（防人工导入直调 parse 绕过 adapter 剔除）；路由：集团专项源不匹配显式 NOT_APPLICABLE（原静默跳过）；测试 194→204 全绿 | 遗留：名单导入入口（Web/CLI 批量导入内部名单文件）留待与 P6 证据系统一起做（名单文件不得入库）；下一步按 WORKPLAN 为 P5 地区插件（四川+1），或按用户任务书执行 | 
| 2026-09-05 (P5 轮) | **P5 地区插件机制完成** | app/sources/regions/{sichuan,guangdong}：四川插件（sc_construction 建筑市场监管资质/安许/诚信双段解析 + sc_credit 信用四川薄封装复用全国契约）、广东插件（gd_construction 复用 jzsc 契约，机制验证样例）；注册表补 sc_credit 条目；base 新增 manual 模式注记（人工核查恒 MANUAL）；红线锁定测试用 ast 扫描核心代码——省名只许出现在注释/docstring，字符串常量/标识符零容忍；docs/PLUGINS.md 插件开发说明（验收清单项落地）；测试 204→214 全绿；路由语义实证：川企投粤项目，注册地与项目地插件源都查（发证地核查原语义不回归） | 遗留：两省平台 URL 人工复核回填+真实联调（P3R 同批待办）；下一步 WORKPLAN 为 P6 证据系统（截图/HTML 留痕+SHA-256），或按用户任务书 |
| 2026-09-05 (P6 轮) | **P6 证据系统完成** | evidence.py 落盘/SHA-256/verify（篡改检出）；AdapterOutcome 带原文；runner 真实链路逐源落证据绑定 query_id（mock 不落盘）；Web 结果页证据回链+原文查看器（防目录穿越）+人工复核流（绑 run_id、审计记录不改判）；bqc import-bans 名单导入口+manual_intake 离线评判闭环（FAIL decision+MANUAL 保留；错主体落库前剔除）；测试 214→221 全绿 | 遗留：截图类证据（浏览器截图）需真机联调配套（P3R）；复核后状态流转（确认→PASS 之类）刻意不做——人工结论是审计记录，改判需显式机制；下一步 WORKPLAN 为 P7 报告（Excel 明细+PDF），或按用户任务书 |
| 2026-09-05 (P7 轮) | **P7 报告完成** | app/reports/excel.py（11 sheet 对应 §16 表 + 状态口径说明 sheet 明示"失败绝不写无异常"红线）+ pdf.py（reportlab CID 中文字体免外部字体文件）+ bqc report 子命令（最新完整批次导出）+ scripts/make_report_samples.py 样张生成（FAIL/ERROR/NO_DATA 三场景，样张在 local/reports/）；openpyxl/reportlab 进运行时依赖、pypdf 进 dev；测试 221→224 全绿 | 遗留：PDF 中文文本抽取依赖阅读器 ToUnicode（pypdf 实测可抽）；样张为 mock 数据可作演示附件；下一步 WORKPLAN 为 P8 Windows 打包（PyInstaller），或按用户任务书 |
| 2026-09-05 (P8 轮) | **P8 Windows 打包完成** | app/paths.py frozen 路径解析（Windows=%LOCALAPPDATA%\bqc\data）；entry_bqc.py+bqc.spec（onefile；动态导入的 10 adapter 显式 hiddenimports；config/templates 随包）；start_server.bat；CI package-windows job（构建→frozen 冒烟含 LOCALAPPDATA 落点断言→zip artifact）；spec 先经本机 Mac 实测（CLI/init-db/serve 全过）再上 CI，一次通过；测试 224→228；Windows 便携 zip 已挂 Release v0.9.0 | 遗留：真机 Windows 双击启动与实际业务数据验收需用户在 Windows 机执行（exe 已带启动脚本）；Defender/SmartScreen 首跑提示属未签名正常现象；下一步 WORKPLAN 为 P9 macOS 包（CI arm64），或按用户任务书 |
| 2026-09-05 (P9 轮) | **P9 macOS 打包完成** | scripts/start_server.command（100755 入库）；CI package-macos job：PyInstaller arm64 构建→frozen 冒烟（init-db 落 Application Support 断言+serve 首页实访问）→zip 保留可执行位→artifact；本机 M4 同 spec 实测（.command 启动/首页 200/数据目录）+CI 产物下载本机复验；测试 228 不变；macOS zip 已挂 Release v0.10.0 | 遗留：Gatekeeper 首跑提示（未签名/未公证）需右键打开或 xattr -dr com.apple.quarantine；下一步 WORKPLAN 仅剩 P10 收尾（Release 自动化/验收表终审），或按用户任务书 |
| 2026-09-05 (P10 轮) | **P10 收尾完成——WORKPLAN 十阶段全部完成** | release.yml：push tag v* 自动执行三平台 pytest 门→wheel/sdist→Windows exe 打包冒烟（LOCALAPPDATA 断言）→macOS arm64 打包冒烟（serve 实访问断言）→自动创建 Release 挂五类产物（wheel/sdist/双平台 zip/SHA256SUMS），说明自动取自 CHANGELOG（scripts/extract_changelog.py，缺失章节显式报错不伪造）；验收表 §22 清单终审：除"已支持数据源清单"（须真实联调后）外全部落实；测试 228 全绿 | **9/20 收口前剩余待办**：①P3R 白天人工复核各源 query_url 回填+真机联调（真实查询的前置，需用户配合）；②Windows/macOS 包真机业务验收（包已就绪）；③可选：代码签名/公证。项目从 9-4 初始化到 9-5 十阶段完成，全程 mock/fixture，零真实站点依赖，红线全数测试锁定 |
| 2026-09-05 (P3R 准备轮) | P3R 前置 | docs/P3R_CHECKLIST.md 逐源复核作业清单（含 2026-09-05 实测风控记录）；bqc check-source 单源联调工具（夜间门控+显式白天覆盖，输出不美化）；docs/SUMMARY.md 项目整体总结；测试 228→233 全绿 | 等：用户按清单人工复核（30~60 分钟）→回填 query_url→逐源联调→解析器修正→真实样本回放 |
| 2026-09-05 (独立复核轮) | 独立复核+修复 | 六项 A 类缺陷：①报告注册表 sheet 恒空（DB 表无写入方）改读包内权威 YAML；②证据写入编码与哈希不一致（代理字符崩溃）统一 replace；③证据落盘失败中断核查→降级告警；④重复提交 500→幂等复用；⑤years_back 服务端钳制 1~10；⑥证据查看器前缀式路径判断→contains 判定；233→238 全绿 | 教训重申：全绿≠无缺陷，本轮全部六项在 233 绿状态下被复核发现 |
| 2026-09-05 (验收准备轮) | 验收材料 | docs/UAT.md 用户验收手册（Windows/macOS/报告证据三组操作单+常见问题）；check-source 离线端到端实测四路径（门控拒绝=2/白天放行=0/未知源拒绝/MANUAL 如实输出）；版本 0.14.0 | 等：用户按 UAT.md 验收双平台包；按 P3R_CHECKLIST.md 人工复核 |
| 2026-09-05 (文档一致性轮) | README 版本漂移修复 | README 状态行停 v0.12.0（实际 0.14.0），即 P0 §六点名的漂移类；修正+新增 test_readme_version_not_stale 锁死；238→239 全绿 |
| 2026-09-05 (侦察工具轮) | P3R 工具 | scripts/recon_portal.py 门户侦察（自动导航+截图+接口候选捕获）；本机假门户离线验证（成功捕获 /api/search?q= 候选+2 截图）；CHECKLIST 增快捷方式；开发依赖不进打包产物 | 用户白天对真实门户运行（一条命令），看图确认 URL 回填 |
| 2026-09-05 (工具链闭环轮) | P3R 工具链闭环 | check-source --save-evidence：联调实测响应原文存为哈希证据（p3r_probe）接入 P6 证据链；修局部 import 遮蔽 init_db 的作用域坑；239→240 全绿 | P3R 工具链齐备：侦察(recon_portal)→回填(人工)→实测留证(check-source --save-evidence)→离线评判(run_check) |
| 2026-09-05 (CLI 收口轮) | CLI 体验 | bqc verify-evidence 证据校验子命令（UAT 两用）；report/import-bans 友好报错（退出码 2 无堆栈）；report 空库先建表；240→241 全绿 |
| 2026-09-05 (公式案例轮·主会话) | 报告增强 | 封面与汇总新增「状态统计」区块：跨表 COUNTIF 逐状态计数 + 数据源 5 态多条件相加 + COUNTA 批次规模 + SUM 同表汇总"异常与待人工合计"（红线口径进公式）；范围按当次明细行数构造（空批次安全不反转）；条款/查询日志状态列条件格式；公式安全契约收紧=真公式只许出现在封面统计区、与 _cell() 消毒互斥双路径；tests/test_excel_formulas.py 5 条回归，275→280 全绿；Mac 实测：样张 LibreOffice headless 重算勾稽一致（FAIL 样张 FAIL=1/查询9源；ERROR 样张数据源异常=1 与 DB 逐条对上） | Windows/macOS 实测=push 后 CI 双平台 job；后续公式需求（如人工复核列联动）待用户反馈 |
| 2026-09-05 (闲时复核轮) | 公式提交复核+修复 | 复核 0e63859（excel.py/test_excel_formulas/test_audit_fixes）：双路径勾稽核验=三场景样张 LibreOffice headless 重算 × DB 直查全部一致（项数=Σ分项、查询项数与异常待人工逐条对上）；发现并修复重要缺陷——封面「状态统计」缺 NO_DATA 逐状态计数行（rules.py 三路径可产出 NO_DATA：未检索到/主管部门正常/他集团禁入，原 9 分项致"条款核查项数">Σ分项勾稽缺口，且 NO_DATA 恰为红线最强调状态）；红测先行 test_cover_stats_enumerate_all_decision_statuses 锁死 Status 九态全枚举；SUM 合计范围改按 MANUAL/UNKNOWN 实际行号锚定（去行号偏移硬编码）；280→281 全绿 | Windows/macOS 实测=本轮 push 后 CI windows-latest/macos-latest job；P3R 真实官网联调仍留白天人工（勿夜间动真实政府网站） |
| 2026-09-08 (离线预警收口) | **新增规则接线与安全/解析口径收口** | F-05/F-06/S-04/P-02/P-03 已由 `review_directory` 主流程产生；F-06 核验 `招标控制价` 为 `kind=control` 且不成为 `primary_quote`；S-04 仅使用 `COMPARABLE` + `comparison_key`；补齐 UTF-16 BOM、欧洲/中文小数解析、JSON 深度/节点预算、XLSX 审计单元格上限，并在 `config.limits` 明示 `max_json_depth`/`max_json_nodes`/`max_xlsx_audit_cells`；新增合成 CSV/relations 集成测试；最终全量实跑结果为 303 passed、1 skipped | **仍无已确认的“同一项目、多家单位投标”真实资料**；新增集成测试仅证明合成输入可从主流程产生信号，不构成真实业务证据；Windows 跳过符号链接测试因当前进程无创建符号链接权限 |
| 2026-09-08 (发布前复核补强) | **解析和相似性边界补强** | 外币/负报价不再静默纳入或丢失；同一投标人存在多个不同控制价时 F-06 保守跳过；短/空文本不触发 S-01；JSON 同时有单价与合价时优先合价；新增 7 条回归测试；全量实跑 309 passed、1 skipped；wheel/sdist、安装包和 CLI 离线烟测通过；GitHub main 的 e15402c（基础版本）对应 CI #37 Success，当前候选未提交变更尚未被该 CI 覆盖；已补齐并验证 CHANGELOG 的 0.21.0 章节提取 | **仍无已确认的真实同项目投标资料；v0.21.0 尚未打 tag/创建 Release，tag/Release 仍需明确授权并在候选提交后由 CI 覆盖** |
| 2026-09-08 (发布前复核第二轮) | **补齐高影响边界的最小防护** | 多数字文本报价保守跳过；去空白摘要改为全文指纹；XLSX 公式不计算并进入独立解析提示；输出阻断输入源硬链接别名；Web 同源校验补协议/端口，`/review-bids` 默认限制在 `BQC_REVIEW_ROOT` 内；新增回归测试后全量 314 passed、1 skipped，离线专项 33 passed、1 skipped | **LAN 入口仍无账号鉴权，只能可信网络临时使用；仍无已确认真实同项目投标资料；当前修改尚未提交并未被 CI 覆盖，v0.21.0 尚未打 tag/创建 Release** |
| 2026-09-08 (继续收口) | **恢复 E-02 并补齐法律/隐私可追溯边界** | 修复未提交差异误删 E-02 的回退；E-02 重新接线并以 E-02 脱敏证据回归锁定；公开 JSON 不再输出完整银行/保证金账号（含 CSV 结构摘要）；报价标签后相邻未标注多数字保守跳过；每条离线信号新增 `legal_basis`，绑定《实施条例》/发改法规规〔2022〕1117号背景并明确仅作线索；全量 317 passed、1 skipped，离线专项 36 passed、1 skipped；compileall 与 `git diff --check` 通过 | **无真实同项目投标资料；无当前提交对应 GitHub CI 结果；LAN 入口仍无账号鉴权，不作为共享生产服务；tag/Release 未创建，需发布授权和固定提交后再做** |
| 2026-09-08 (固定提交与制品预检) | **候选固定并完成本机制品预检** | 提交 `b8c9a7d` 固定 E-02 脱敏、法律/政策背景和验收文档；全量 317 passed、1 skipped；compileall、`git diff --check`、`pip check` 通过；wheel/sdist 均构建成功，并分别在隔离环境安装后通过 `smoke_installed.py`；`origin/main` 仍为 `4f61ed0`，未推送 | **当前提交尚无 GitHub CI 结果；无真实同项目投标资料；LAN 入口仍无账号鉴权；v0.21.0 tag/Release 未创建，需明确授权后再做** |
| 2026-09-08 (自审修复与 CI 收口) | **关闭 v0.21.1 自审发现的四类边界缺陷并补齐现有 PDF 输入支持** | 修复 XLSX 原生数值小数位误读、公开原文账号泄露、P-04 冲突字段不确定取值和配对别名重复触发；保留 PDF 文本层解析并增加回归测试；本机全量 325 passed、1 skipped；自审脚本 10/10 通过；提交 `956b25e` 的 GitHub CI 三平台各 326 passed，通用构建、wheel/sdist 隔离烟测及 Windows/macOS 打包烟测通过；v0.22.0 已推送 main | **仍无真实同项目投标资料；LAN 入口仍无账号鉴权；不新建 tag/Release** |
| 2026-09-08 (v0.23.0-rc.1 OCR 收口) | **PDF 扫描件内置 OCR 候选完成** | PDF 改为流式摘要和逐页读取；无文本或含大图/Form 页以 Poppler+Tesseract 本机识别，报价保留页码/识别方式，OCR 原文不公开。2000 页/50 OCR 页/120 秒/1000 万字符预算及失败、空页、未处理页均显式标为部分解析，部分解析文件不进入全文相似度比较；修复“歧义小数 + 第二数字”被误认报价，以及混合页 OCR 与文本层报价冲突时覆盖文本层的边界。Windows/macOS 发布构建纳入引擎与 `chi_sim`/`eng` 语言包，并增加清空外部 PATH 的扫描件烟测；本机全量 `333 passed, 1 skipped`，真实中文扫描/同页图文 OCR 实测通过 | **当前为本机候选，尚待本轮提交后的 GitHub 三平台构建；OCR 金额、单位和表格归属仍必须逐页人工核对；真实同项目投标资料、LAN 入口鉴权和 P3R 官网联调仍未确认** |
| 2026-09-18 (界面验收修复轮) | **Web 离线审查核心流程修复 + 错误态 HTML 化 + 状态配色语义修正** | 真实浏览器端到端验收（ZCode 内置浏览器）：①发现并修复 `_confine_review_path` 运算符优先级缺陷——`inside = path == root if allow_root else ...` 使 allow_root=True 时仅放行根目录本身，/review-bids 传任何**子目录**均 400（自 09-08 限根加固起核心流程不可用，原测试只覆盖根本身与外部路径恰好漏过）；②表单校验失败由裸 JSON 改为 400 + review.html 错误卡（原因/指引/表单保留可修正重试）；③ERROR/TIMEOUT/BLOCKED 徽章 #7f8c8d→#a04000，脱离与 NO_DATA(#95a5a6) 同灰系——查询失败不再看着像"没查到"（红线配色语义）；④base.html 补 ≤640px 单列媒体查询，窄屏表单不再截断。新增 3 项回归测试并强化 1 项既有断言（外部路径 400 由 JSON 契约更新为 HTML 契约，语义未弱化），全量 **336 passed, 1 skipped**；2026-09-18 独立复核通过（review_report_20260918.md）；浏览器实测：子目录审查出 F/S 信号、错误页可读可重试、375px 单列正常 | LAN 入口无鉴权、P3R 官网联调仍待白天人工（沿用前轮结论）；本分支待推送后 CI 三平台覆盖 |
| 2026-09-20 (rc.4 缺陷修复轮) | **离线审查三类真实缺陷修复：幽灵投标人假信号/纵排转置报价漏报/关联线索歧义留痕** | 合成输入对抗性探针（12+10 例，全部临时目录、零真实资料）实锤并修复：①根目录无「投标人__文件名」前缀文件此前按文件名拆成多家投标人互相比较——同一投标人「报价.txt+施工组织.md」会凭空产生假 F-01「报价↔施工组织」，现按文档口径归入根目录名单一投标人；②纵排（标签行+下行金额）与转置（标签行+数值行）汇总表此前产不出任何报价导致漏报，现按位置一一配对（控制价仍 kind=control 不作主报价），数值行后紧跟数字列/税率语义/多数字一律放弃宁漏勿错，证据保留标签行+金额行原文行号；③关联线索 CSV 重复表头、JSON 非对象行由静默处理改为 parse_warnings 留痕。新增 7 条回归测试，版本 0.23.0rc4 三处同步 | 真实官网联调 P3R、LAN 鉴权、真实同项目资料仍缺（沿用前轮结论）；姊妹工作树 2026-09-07 local/real_test 有真实商业资料（真实公司名/金额），按红线未引入仓库，脱敏转化留待有权限人员确认 |
| 2026-09-20 (rc.4 收口核验轮) | **收口核验+文案一致性修订+运行时产物排除** | ①总控转来的"译文审核"任务经全机定位（任务索引/会话记录/Documents/Codex/盘符）不存在且不归属本仓库，未臆改仓库外文本；②按其核对维度完成本仓库一致性审计：CLI 7 子命令与选项、18 规则编号、F-01~F-07/S-02 阈值、BQC_* 环境变量、支持格式、UI 产品名、CHANGELOG 提取脚本逐项与代码一致；修复 ACCEPTANCE/SUMMARY 两处 v0.22.0 时代测试统计与"rc.1 当前候选"表述漂移（add2e34）；③远端状态证据：reflog 记录 2026-09-20 01:29:45 (+0800) 本机发生一次非本会话操作的 push，前 3 个提交（0cb1a94/941d66d/be2e806）已在 GitHub origin/main；本地遗留 add2e34 待 09-21 08:00 门槛，无分叉（HEAD..origin/main 为空）；④收口核验全绿：全量 342 passed/2 skipped，wheel/sdist 构建成功，秘密扫描（待推 diff+全跟踪树）干净；⑤.mimosa//.v2c/ 为 Mimosa 钩子与插件活动运行时状态，选排除不清理，已加入 .gitignore | 遗留不变：P3R 官网联调待白天人工、LAN 鉴权、真实同项目资料；浏览器端 TS 解析器暂无纵排/转置配对能力（规则级对齐不受影响），是否移植待定 |
